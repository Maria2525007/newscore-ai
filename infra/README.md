# infra/ — заглушка

Инфраструктура (Docker prod, K8s, Yandex Cloud, CI/CD) появится в **Step 5** (продуктивизация). См. `raw/roadmap.md`.

На Step 0 запуск локальный: `uv sync && uv run newscore "запрос"`.
